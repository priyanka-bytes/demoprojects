package com.bytesedge.demo.utils;

import java.util.List;

public class AppUtils {

	public static <T> String getObjectIds(List<T> lst, Class<T> cls) {
		String tgt = "";
		
		for(T e: lst) {
			if(cls.isAssignableFrom(cls.getClass())) {
				
			}
		}
		
		
		return tgt;
	}
}
